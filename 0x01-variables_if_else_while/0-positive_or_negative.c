#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - Prints a number if it is positive, zero or negative
 *
 * Return: Always (Success)
 */
int main(void)
{
	int n;

	srand(time(0));
	n=rand() - RAND_MAX / ;

	if(n>0)
	{
		printif(
